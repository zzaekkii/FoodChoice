version: "3"

services:
  app:
    container_name: foocho-app
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "8080:8080"
    command: sh -c "python3 /app/db.py && python3 /app/app.py"